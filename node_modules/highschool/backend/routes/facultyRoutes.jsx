import express from 'express';
import { addFaculty, getFaculties } from '../controllers/facultyController.js';
const router = express.Router();

router.post('/', addFaculty);
router.get('/', getFaculties);

export default router;